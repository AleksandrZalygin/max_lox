entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
          permanent: true
  websecure:
    address: ":443"

providers:
  docker:
    exposedByDefault: false
    network: proxy

certificatesResolvers:
  letsencrypt:
    acme:
      email: maks.kotov38@@gmail.com
      storage: /acme.json
      httpChallenge:
        entryPoint: web

api:
  dashboard: false

log:
  level: INFO
